// Detener la animación para que se quede en el frame 0 por defecto
image_speed = 0;
image_index = 0;

// Variable para definir el destino (puedes cambiarla desde el editor de salas)
target_room = Room2